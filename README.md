
  # Dark Theme Student Dashboard

  This is a code bundle for Dark Theme Student Dashboard. The original project is available at https://www.figma.com/design/ZsUIjgy0Wu9jThO2O53q3B/Dark-Theme-Student-Dashboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  